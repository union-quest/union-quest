type Player @entity {
  id: ID!
  x: Int!
  y: Int!
  xDestination: Int
  yDestination: Int
  arrivalTime: Int
  workTime: Int
}

type Village @entity {
  id: ID!
  x: Int!
  y: Int!
  name: String!
  description: String!
  member: String!
}

type ItemType @entity {
  id: ID!
  name: String!
  description: String!
  buyPrice: BigInt!
  sellPrice: BigInt!
}

type Trust @entity {
  id: ID!
  staker: Player!
  borrower: Player!
  trustAmount: BigInt!
}
