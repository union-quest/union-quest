type Player @entity {
  id: ID!
  x: Int!
  y: Int!
  balance: Int!
  xDestination: Int
  yDestination: Int
  arrivalTime: Int
}

type Village @entity {
  id: ID!
  x: Int!
  y: Int!
  name: String!
  description: String!
}
