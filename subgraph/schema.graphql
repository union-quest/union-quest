type Item @entity {
  id: ID!
  name: String!
  symbol: String!
  stake: BigInt!
  tools: [Item!]!
  outputRecipes: [Recipe!]! @derivedFrom(field: "output")
  inputRecipes: [Input!]! @derivedFrom(field: "item")
  balances: [Balance!]! @derivedFrom(field: "item")
}

type Input @entity {
  id: ID!
  item: Item!
  recipe: Recipe!
  quantity: BigInt!
}

type Recipe @entity {
  id: ID!
  inputs: [Input!]!
  output: Item!
}

type Player @entity {
  id: ID!
  startX: BigInt!
  startY: BigInt!
  endX: BigInt!
  endY: BigInt!
  startTimestamp: BigInt!
  vouch: BigInt!
  balances: [Balance!]! @derivedFrom(field: "player")
}

type Balance @entity {
  id: ID!
  item: Item!
  player: Player!
  value: BigInt!
  skill: BigInt!
}
