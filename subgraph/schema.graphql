type Tile @entity {
  id: ID!
  x: BigInt!
  y: BigInt!
  resourceId: BigInt!
}

type Player @entity {
  id: ID!
  startTile: Tile!
  endTile: Tile!
  startTimestamp: BigInt!
  woodSkill: BigInt!
  stoneSkill: BigInt!
  vouch: BigInt!
}
